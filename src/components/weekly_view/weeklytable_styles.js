import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  iconColoured: {
    cursor: "pointer",
    color: "#b70202",
  },
  fontWeight: {
    fontWeight: "600",
  },
}));

export default useStyles;
